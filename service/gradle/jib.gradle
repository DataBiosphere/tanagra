jib {
    from {
        // Google's distroless images are openjdk, this is the simplest with adoptopenjdk
        image = 'us.gcr.io/broad-dsp-gcr-public/base/jre:17-debian'
    }
    container {
        mainClass = 'bio.terra.tanagra.app.Main'
    }
}
