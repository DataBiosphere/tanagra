package bio.terra.tanagra.api.accesscontrol;

/** Names of actions users can take on Tanagra resources. */
public enum Action {
  READ,
  QUERY_INSTANCES,
  QUERY_COUNTS,
  CREATE,
  UPDATE,
  DELETE
}
