spotless {
    groovyGradle {
        target '**/*.gradle'
        greclipse()
        indentWithSpaces()
    }
    java {
        googleJavaFormat()
        removeUnusedImports()
        targetExclude "${project.buildDir}/**"
    }
}

compileJava.dependsOn spotlessApply
